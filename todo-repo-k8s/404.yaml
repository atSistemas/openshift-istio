apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: todo-repo
spec:
  hosts:
  - todo-repo
  http:
  - fault:
      abort:
        percent: 20
        httpStatus: 400
    route:
    - destination:
        host: todo-repo
        subset: v1
